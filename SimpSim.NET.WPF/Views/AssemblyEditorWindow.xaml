<Window x:Class="SimpSim.NET.WPF.Views.AssemblyEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:SimpSim.NET.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Assembly Editor" Height="300" Width="300">
    <Window.DataContext>
        <viewModels:AssemblyEditorWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding AssemblyEditorText}" />
        <Button Grid.Row="1" Command="{Binding AssembleCommand}">Assemble</Button>
    </Grid>
</Window>











