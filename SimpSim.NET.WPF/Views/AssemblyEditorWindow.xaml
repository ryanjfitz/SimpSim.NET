<Window x:Class="SimpSim.NET.WPF.Views.AssemblyEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Assembly Editor" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding AssemblyEditorText}" AcceptsReturn="True" />
        <TextBlock Grid.Row="1" Foreground="Red" Text="{Binding AssemblyErrorMessage}" />
        <Button Grid.Row="2" Command="{Binding AssembleCommand}">Assemble</Button>
    </Grid>
</Window>
